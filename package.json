{
    "name": "shell-assistant",
    "type": "module",
    "version": "1.0.0",
    "main": "src/assistant.ts",
    "scripts": {
        "start": "yarn run build && node dist/bundle.js",
        "test": "vitest run",
        "build": "vite build"
    },
    "dependencies": {
        "dotenv": "^16.0.3",
        "openai": "^4.0.0"
    },
    "devDependencies": {
        "ts-node": "^10.9.1",
        "typescript": "^5.8.3",
        "vite": "^6.3.5",
        "vitest": "^1.0.0"
    }
}
